<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Ritmo_App.IslandNotesWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Ritmo_App"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="Root" RequestedTheme="Dark" Background="#16161A" Padding="18" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Cabecera: ámbito (sesión activa o generales) -->
        <StackPanel Grid.Row="0" Spacing="2">
            <StackPanel Orientation="Horizontal" Spacing="8">
                <FontIcon Glyph="&#xE70B;" FontSize="16" Foreground="{ThemeResource AccentFillColorDefaultBrush}" VerticalAlignment="Center" />
                <TextBlock x:Name="ScopeText" Text="Notas" FontSize="17" FontWeight="SemiBold" VerticalAlignment="Center" />
            </StackPanel>
            <TextBlock x:Name="ScopeHint" Text="" FontSize="11" Opacity="0.55" TextWrapping="Wrap" />
        </StackPanel>

        <!-- Editor: nueva nota o edición de una existente -->
        <StackPanel Grid.Row="1" Spacing="8">
            <TextBox x:Name="TitleBox" PlaceholderText="Título de la nota" />
            <TextBox x:Name="ContentBox" PlaceholderText="Escribe aquí… (admite Markdown)"
                     AcceptsReturn="True" TextWrapping="Wrap" Height="96"
                     ScrollViewer.VerticalScrollBarVisibility="Auto" />
            <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <Button x:Name="SaveBtn" Click="SaveBtn_Click" Style="{StaticResource AccentButtonStyle}">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE74E;" FontSize="13" />
                        <TextBlock x:Name="SaveBtnText" Text="Guardar nota" />
                    </StackPanel>
                </Button>
                <Button x:Name="NewBtn" Content="Nueva" Click="NewBtn_Click" Visibility="Collapsed" />
                <TextBlock x:Name="StatusText" VerticalAlignment="Center" Opacity="0.8" FontSize="12" />
            </StackPanel>
        </StackPanel>

        <!-- Notas existentes de esta sesión (o generales) -->
        <Grid Grid.Row="2" RowSpacing="6">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="EXISTENTES" FontSize="10" FontWeight="SemiBold" Opacity="0.5" CharacterSpacing="80" />
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="NotesList" Spacing="8" />
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>
