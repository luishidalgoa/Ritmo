<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="Ritmo_App.Dialogs.PhaseDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Ritmo_App.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Fase del plan"
    PrimaryButtonText="Guardar"
    SecondaryButtonText="Cancelar"
    DefaultButton="Primary">

    <StackPanel Spacing="12" Width="320">
        <TextBox x:Name="NameBox" Header="Nombre" PlaceholderText="p. ej. Segunda vuelta" />
        <CalendarDatePicker x:Name="FromPicker" Header="Desde" HorizontalAlignment="Stretch" />
        <ToggleSwitch x:Name="OpenEndedSwitch" Header="Sin fecha de fin (indefinida)"
                      OffContent="No" OnContent="Sí" Toggled="OpenEndedSwitch_Toggled" />
        <CalendarDatePicker x:Name="ToPicker" Header="Hasta" HorizontalAlignment="Stretch" />
        <TextBlock x:Name="ErrorText" Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                   FontSize="12" TextWrapping="Wrap" Visibility="Collapsed" />
    </StackPanel>
</ContentDialog>
