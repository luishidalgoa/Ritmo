<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="Ritmo_App.Dialogs.SessionBehaviorDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Ritmo_App.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Qué se abre al concentrarme"
    PrimaryButtonText="Guardar"
    SecondaryButtonText="Cancelar"
    DefaultButton="Primary">

    <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="460">
        <StackPanel Spacing="12" Width="340">
            <TextBlock x:Name="TitleText" FontSize="16" FontWeight="SemiBold" TextWrapping="Wrap" />
            <TextBlock x:Name="EnvText" Opacity="0.7" FontSize="13" TextWrapping="Wrap" />
            <ToggleSwitch x:Name="CustomizeSwitch" Header="Personalizar (si no, abre todo)"
                          OffContent="Abre todo" OnContent="Subconjunto" Toggled="CustomizeSwitch_Toggled" />
            <StackPanel x:Name="ListsPanel" Spacing="8" />
            <TextBlock x:Name="EmptyHint" Opacity="0.6" FontSize="12" TextWrapping="Wrap" Visibility="Collapsed" />
        </StackPanel>
    </ScrollViewer>
</ContentDialog>
