<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Ritmo_App.TasksPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Ritmo_App"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="290" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Columna izquierda: lista de bloques (cada uno abre su vista completa a la derecha). -->
        <Grid Grid.Column="0" Padding="16" RowSpacing="12"
              Background="{ThemeResource LayerFillColorDefaultBrush}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Spacing="2">
                <TextBlock Text="Tareas" FontSize="22" FontWeight="SemiBold" />
                <TextBlock Text="Tus listas, estilo Recordatorios." Opacity="0.7" FontSize="12" />
            </StackPanel>
            <Button Grid.Row="1" x:Name="AddBlockBtn" Content="Nuevo bloque" Click="AddBlockBtn_Click"
                    HorizontalAlignment="Stretch" Style="{ThemeResource AccentButtonStyle}" />
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="BlockListHost" Spacing="4" />
            </ScrollViewer>
        </Grid>

        <!-- Columna derecha: vista completa del bloque seleccionado. -->
        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="DetailHost" Padding="32" Spacing="16" MaxWidth="820" HorizontalAlignment="Stretch" />
        </ScrollViewer>
    </Grid>
</Page>
